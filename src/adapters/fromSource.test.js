describe("fromSource()", () => {
  it("adds all types", () => {});
  it("removes all unknown types", () => {});
  it("groups adjacent top level items into discrete folders", () => {});
  it("wraps single top level items in discrete folders", () => {});
  it("adds breadcrumbs too all items", () => {});
});
