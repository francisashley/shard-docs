## Install

```bash
yarn add @fa-repo/shard-docs
```

## Basic usage

```jsx
// index.js
import React from "react";
import { render } from "react-dom";

import ShardDocs from "@fa-repo/shard-docs";
import "@fa-repo/shard-docs/dist/index.css";

const data = [
  { name: "Install", content: <h1>Install</h1> },
  { name: "Basic usage", content: <h1>Basic usage</h1> },
  {
    name: "Examples",
    content: [
      { name: "Use case A", content: <h1>Use case A</h1> },
      { name: "Use case B", content: <h1>Use case B</h1> }
    ]
  },
  { name: "Github", content: "https://github.com" },
]

const root = document.getElementById("root");

render(<ShardDocs title="ShardDocs demo" data={data} />, root);
```

### Shards

ShardDocs comes bundles with a couple of components for adding visual sugar to the docs. Check them out [here](#/shards).